namespace Game.Clans
{
    public enum ClanRole
    {
        Leader,
        CoLeader,
        Elder,
        Member
    }

    public enum WarFrequency
    {
        Never,
        Rarely,
        Sometimes,
        Often,
        Always
    }
}
